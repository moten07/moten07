## Jetpack Compose

### 重组

- 重组是 Jetpack Compose 的核心概念之一，它指的是在 UI 发生变化时，重新计算和更新 UI 的过程。当状态发生变化时，Compose
  会自动触发重组，以确保 UI 与最新的状态保持同步。
- 重组的过程是高效的，因为 Compose 只会重新计算和更新那些受影响的 UI 元素，而不是整个 UI。这使得 Compose
  能够在性能方面表现出色，尤其是在处理复杂的 UI 时。
- 重组的触发条件包括状态变化、输入事件、布局变化等。当这些条件满足时，Compose 会自动进行重组，以确保 UI 的正确性和响应性。

#### 重组的优化

- Compose 通过智能地管理重组来优化性能。例如，Compose 会使用“快照”机制来跟踪状态的变化，并且只在必要时触发重组。这意味着如果某个状态没有发生变化，相关的
  UI 元素将不会被重新计算和更新。
- 开发者可以通过使用 `remember` 和 `derivedStateOf` 等 API 来进一步优化重组。这些 API 可以帮助开发者缓存计算结果，避免不必要的重组，从而提高性能。
- 此外，Compose 还提供了工具和调试功能，帮助开发者分析和优化重组过程。例如，开发者可以使用 Android Studio 的布局检查器来查看哪些
  UI 元素正在被重组，以及重组的频率和原因。

### 副作用

- 副作用是指在 Compose 中执行的那些可能会影响 UI 以外的操作，例如网络请求、数据库访问、日志记录等。副作用通常需要在特定的生命周期阶段执行，以确保它们不会干扰
  UI 的正常运行。
- Compose 提供了 `LaunchedEffect`、`SideEffect` 和 `DisposableEffect` 等 API 来处理副作用。这些 API
  允许开发者在特定的条件下执行副作用，并且可以确保它们在适当的时间被清理和取消。

### 状态提升

- 状态提升是指将状态从一个组件提升到其父组件，以便在多个子组件之间共享状态。这是一种常见的设计模式，可以帮助开发者更好地管理状态和数据流。
- 通过状态提升，开发者可以避免在多个组件之间重复定义状态，并且可以更容易地实现组件之间的通信和数据共享。这有助于提高代码的可维护性和可读性。
- 在 Compose 中，状态提升通常通过将状态作为参数传递给子组件来实现。父组件可以管理状态的生命周期，并且可以根据需要更新状态，从而触发子组件的重组和
  UI 更新。
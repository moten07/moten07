## Java基础

### 线程与进程

#### 1. 线程和进程的区别

- 进程是操作系统分配资源的基本单位，每个进程都有独立的内存空间和系统资源；线程是进程中的一个执行单元，多个线程共享同一进程的内存空间和系统资源。
- 进程之间相互独立，一个进程崩溃不会影响其他进程；线程之间相互依赖，一个线程崩溃可能会导致整个进程崩溃。
- 进程切换的开销较大，因为需要保存和恢复进程的状态；线程切换的开销较小，因为线程共享同一进程的内存空间和系统资源。
- 进程之间的通信需要使用进程间通信机制，如管道、消息队列、共享内存等；线程之间的通信可以直接通过共享内存来实现。

#### 2. Android中的线程和进程

- Android应用程序默认运行在一个进程中，主线程（UI线程）负责处理用户界面和用户交互，其他线程（如工作线程）负责处理耗时的操作，如网络请求、数据库操作等。
- Android允许应用程序创建多个线程来处理不同的任务，但所有线程都共享同一进程的内存空间和系统资源。
- Android还允许应用程序创建多个进程来运行不同的组件，如Activity、Service等，但这些进程之间相互独立，无法直接共享内存空间和系统资源。
- 在Android中，主线程（UI线程）是一个特殊的线程，负责处理用户界面和用户交互，任何耗时的操作都应该在其他线程中执行，以避免阻塞主线程导致ANR（Application
  Not Responding）错误。
- Android提供了多种机制来创建和管理线程，如Thread、HandlerThread、AsyncTask、ExecutorService等，开发者可以根据具体的需求选择合适的线程管理方式.

#### 3. 线程同步和线程安全

- 线程同步是指多个线程在访问共享资源时，确保只有一个线程能够访问该资源，以避免数据不一致和竞争条件。常用的线程同步机制包括synchronized关键字、Lock接口、Semaphore等。
- 线程安全是指一个类或方法在多线程环境下能够正确地工作，不会出现数据不一致或竞争条件。要实现线程安全，可以使用线程同步机制来保护共享资源，或者使用不可变对象、局部变量等方式来避免共享资源的访问。
- 在Android中，开发者需要特别注意线程安全问题，尤其是在处理UI组件时，因为UI组件只能在主线程中访问，如果在其他线程中访问UI组件，可能会导致应用程序崩溃或出现不可预测的行为。
- 为了确保线程安全，开发者可以使用Handler或其他机制将结果传回主线程更新UI，或者使用线程同步机制来保护共享资源，避免数据不一致和竞争条件。
- 在Android中，开发者还可以使用一些线程安全的类和方法，如AtomicInteger、ConcurrentHashMap等，这些类和方法已经内部实现了线程安全，可以直接使用，避免了手动实现线程同步的复杂性。
- 在Android中，开发者还可以使用一些第三方库来简化线程管理和线程安全的实现，如RxJava、Kotlin
  Coroutines等，这些库提供了丰富的功能和工具来处理多线程编程，帮助开发者更轻松地实现线程安全和异步操作。
- 总之，线程同步和线程安全是多线程编程中的重要概念，开发者需要理解它们的原理和应用场景，并且在Android开发中合理使用线程管理机制和线程安全的类和方法，以确保应用程序的稳定性和性能。

### Java内存模型

### GC